.DS_Store
node_modules/
# The variable brand face ships with the tool so a deployed copy can set the type.
# The other cuts are unused — fonts/fonts.json loads the variable one only — so they stay out.
fonts/*.otf
fonts/*.ttf
fonts/*.woff
fonts/*.woff2
!fonts/NeueDisplayNextVariable.ttf
